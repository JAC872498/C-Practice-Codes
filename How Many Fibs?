/**/

#include <iostream>

int fibs(int num){
	if(num<=1) return(1);
	return(fibs(num-2)+fibs(num-1));
}

int main(){
	int a=1,b=1,n=0,ways=0;
	std::cin>>a>>b;
	while(a!=0||b!=0){
		while(fibs(n)<=b){
			if(fibs(n)>=a) ways+=1;
			n+=1;
		}
		std::cin>>a>>b;
		std::cout<<ways;
		ways=0;
	}
}
